#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int max3(int x, int y, int z)
{
	int		max, res;
	max = x;
	if (x > y)
}
/*
int main(void)
{
	int		a, b, c, m;
	scanf("%d %d %d", &a, &b, &c);
	m = max3(a, b, c);
	printf("%d", m);
	return 0;
}
