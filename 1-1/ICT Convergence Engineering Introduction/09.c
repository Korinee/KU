#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

#define			PI 3.141592

int main()
{
	double		x1, y1, x2, y2;
	double		distance = 0.;

	distance = sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1))
		printf
		("area: %f \n", distance);

	return 0;

}